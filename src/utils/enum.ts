export enum TypeQuestion {
    TEXT = 'TEXT',
    SINGLE = 'SINGLE',
    MULTIPLE = 'MULTIPLE'
}

export enum TypePage {
    QUESTION = 'QUESTION',
    MESSAGE = 'MESSAGE',
}

export enum IStatusSurvey {
    OPENED = 'OPENED',
    CLOSED = 'CLOSED',
  }